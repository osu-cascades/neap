<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>NIEAP - Energy Applications</title>
    </head>
    <body>
        <h1>Edit Energy Application</h1>
        <p> Energy Application <%= @enApp.id %> <p>
        <% clientName = "[NO CLIENT]" %>
        <% clientID = "" %>
        <% if @client != nil then clientName = (@client.first_name + " " + @client.last_name) end %>
        <% if @client != nil then clientID = @client.id end %>
        <p> Client - <%=  clientName %> - clientID <%= @enApp.clientID %></p>
        <div>
            <% formStr = "/energy_applications/" + @enApp.id.to_s + "/update" %>
            <form action=<%= formStr %> method="get">
                <label for="fcid">Client ID:</label><br>
                <input type="text" id="fcid" name="fcid" value=<%= clientID %>><br>
                <button type="submit" class="btn btn-primary" id="updateRecordButton" style="margin-top:.5em">Update</button>
            </form>
        </div>
        <div>
            <% formStr = "/energy_applications/" + @enApp.id.to_s + "/remove" %>
            <form action=<%= formStr %> method="get">
                <button type="submit" class="btn btn-primary" id="deleteRecordButton" style="margin-top:.5em">Delete</button>
            </form>
        </div>
    </body>
</html>
