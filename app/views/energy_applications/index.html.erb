<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>NIEAP - Energy Applications</title>
    </head>
    <body>
        <h1>Energy Applications</h1>
        <div class="row">
            <div class="column">
                <div class="row">
                    <button class="btn btn-primary" id="createRecordButton">Create Record</button>
                </div>
                <div class="row">
                    <button class="btn btn-primary" id="updateRecordButton">Update Record</button>
                </div>
                <div class="row">
                    <button class="btn btn-primary" id="destroyRecordButton">Destroy Record</button>  
                </div>  
            </div> <!-- end first column -->
            <div class="column">
                <ul>
                    <% @enAppList.each do |enApp| %>
                        <li>
                        <%= "Energy Application ID: " + enApp.id.to_s %>
                        <% userName = User.find_by(id: enApp.clientID) %>
                        <% if userName == nil then userName = "[No Name]" end%>
                        <%= ", User: " + userName %>
                        </li>
                    <% end %>
                </ul>
            </div>  <!-- end second column -->
        </div>
    </body>
</html>
