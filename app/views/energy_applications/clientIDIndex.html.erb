<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>NIEAP - Energy Applications - Clients by ID</title>
    </head>
    <body>
        <h1>Energy Applications</h1>
        <div class="row">
            <div class="column" style="width: 25%"> 
                <form action="/home" method="get">
                    <button type="submit" class="btn btn-primary" id="homeButton" style="margin-top:.5em">Home</button>
                </form> 
            </div> <!-- end first column -->
            <div class="column">
                    <% @clientList.each do |client| %>
                        <% buttonStr = "ClientID: " + client.id.to_s %>
                        <% userName = "[UNKNOWN CLIENT]" %>
                        <% if client.first_name != nil then userName = client.first_name + " " + client.last_name end%>
                        <% formStr = "" %> <!-- fill in later if need to manage applicants -->
                        <div>
                            <form action=<%= formStr %> method="get">
                                <button type="submit" class="btn btn-primary" style="margin-top:.5em" >
                                    <%= buttonStr + ", " + userName %>
                                </button>
                            </form>
                        </div>
                    <% end %>
            </div>  <!-- end second column -->
        </div>
    </body>
</html>
